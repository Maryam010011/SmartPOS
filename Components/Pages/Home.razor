@page "/"
@inject NavigationManager NavigationManager

<PageTitle>SmartPOS+ | Redirecting...</PageTitle>

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/login", replace: true);
    }
}
