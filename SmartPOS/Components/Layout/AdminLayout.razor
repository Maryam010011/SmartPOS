@* ─────────────────────────────────────────────────────────────────────
   DEPRECATED — This stub has been superseded by:
   Components/Layout/Admin/AdminLayout.razor  (@namespace SmartPOS.Components.Layout)

   This file is kept only to prevent missing-file errors from the solution
   explorer. It intentionally uses a different namespace so it does NOT
   conflict with the real AdminLayout class.
   ───────────────────────────────────────────────────────────────────── *@
@namespace SmartPOS.Components.Layout.Legacy
@inherits LayoutComponentBase

@Body